import { Router } from "express";
import User from '../db/models/user.model';

const app = Router();

// app.get("/me", (req, res) => {
//   try {
//     const instance
//   } catch
// })

export default app;
